LET price = 100;
LET volume = 5;
LET threshold = 450;
LET total = price * volume;

IF (total > threshold) {
    EMIT BUY volume;
}

LET correction = total - 300;

IF (correction < 100) {
    EMIT SELL 3;
}

LOOP 2 TIMES {
    EMIT BUY 1;
}
